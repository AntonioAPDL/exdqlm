# Consolidation Scope Update: Include 0.6.0 Static Regression

Date (UTC): 2026-02-10 06:00:12
Branch: `integrate/v0.4.0-on-v0.3.0`

## What changed
- Consolidation plan now targets one CRAN submission that bundles `0.4.0 + 0.5.0 + 0.6.0` features.
- Phase mapping is corrected so:
  - `0.5.0` integration is synthesis-only.
  - `0.6.0` integration is static regression + `regMod` (+ related multireg/static-qr work once audited).

## Rationale
- Existing Phase 5.1 audit confirms `origin/feature/0.5.0-syntheis` contains synthesis-focused additions and does not provide `regMod` or a clean static-regression VB/MCMC feature block.
- Static regression scope belongs to the `0.6.0` lineage and requires a dedicated audit/integration phase.

Reference audit:
- `tools/merge_reports/20260209-190430_phase5_audit_0.5.0_syntheis.md`

## Consequences for implementation flow
- Phase 5 remains synthesis-only integration using the already-audited selective port strategy from `87eabd2`.
- New Phase 6 is reserved for audit + selective integration of `origin/feature/0.6.0-multireg-static-qr`.
- Final submission branch target is updated to `cransub/0.6.0` (cut from `integrate/v0.6.0-on-v0.5.0`).

## Scope guardrails (unchanged)
- No broad merge from historical branches.
- Selective replay only, with explicit do-not-import churn policy.
- API compatibility with current builder/composition conventions is preserved unless explicitly validated.
