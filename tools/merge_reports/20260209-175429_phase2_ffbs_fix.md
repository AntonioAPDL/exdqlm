# Phase 2 implementation gate: FFBS indexing parity (items 2.3-2.5)

## Branch snapshot
- Branch: `integrate/v0.4.0-on-v0.3.0`
- HEAD at report write: `edddcb7`

## What changed
- Discovery report cleanup (doc-only): `tools/merge_reports/20260209-171723_phase2_ffbs_discovery.md` (commit `6280014`).
- R FFBS indexing fix in fallback paths:
  - `R/exdqlmISVB.R`: smoothing gain transition changed from `t(GG[,,t])` to `t(GG[,,(t+1)])`.
  - `R/exdqlmLDVB.R`: smoothing gain transition changed from `t(GG[,,t])` to `t(GG[,,(t+1)])`.
  - `R/exdqlmMCMC.R`: smoothing gain transition changed in all backward loops; covariance transition in sampler loops changed from `GG[,,t]` to `GG[,,(t+1)]`.
- Loop bounds were not changed; all affected loops are `for(t in (TT-1):1)`, so `(t+1)` is valid.

## Regression test added
- File: `tests/testthat/test-ffbs-indexing-parity.R` (commit `edddcb7`).
- Test intent:
  - constructs a tiny, time-varying, non-identity `GG` model (`TT = 10`, `p = 2`),
  - runs `exdqlmISVB` twice with fixed seed,
  - forces R fallback via `options(exdqlm.use_cpp_kf = FALSE)` and C++ bridge via `TRUE`,
  - checks parity of smoothed means (`theta.out$sm`) and one covariance diagonal trace (`theta.out$sC[1,1,]`) with tolerance `1e-6`.

## Phase gate results (single run)
- Log folder: `check-logs/20260209-174126-phase2-ffbs-fix`
- `devtools::test()`:
  - Result: `[ FAIL 0 | WARN 0 | SKIP 0 | PASS 847 ]`
  - Log: `check-logs/20260209-174126-phase2-ffbs-fix/devtools-test.log`
- `devtools::check(args="--as-cran", cran=TRUE)`:
  - Result: `0 errors | 0 warnings | 3 notes`
  - Log: `check-logs/20260209-174126-phase2-ffbs-fix/devtools-check-as-cran.log`
  - Notes (expected/environment/toolchain):
    - installed package size (`libs` size),
    - unable to verify current time,
    - non-portable compilation flags from system toolchain.

## Commit summary for this phase chunk
- `6280014` — `docs: clean phase-2 FFBS discovery report formatting`
- `375b7ba` — `fix: align R FFBS smoothing gain to use GG[t+1]`
- `edddcb7` — `test: add FFBS indexing parity check (R vs C++)`
