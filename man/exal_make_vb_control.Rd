% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exal_inference_config.R
\name{exal_make_vb_control}
\alias{exal_make_vb_control}
\title{Build advanced VB control}
\usage{
exal_make_vb_control(
  max_iter = 150L,
  min_iter_elbo = 10L,
  tol = 1e-04,
  tol_par = NULL,
  n_samp_xi = 500L,
  verbose = FALSE,
  sigmagam = NULL,
  sts = NULL,
  rhs = NULL,
  diagnostics = NULL
)
}
\arguments{
\item{max_iter, min_iter_elbo, tol, tol_par, n_samp_xi, verbose}{Core VB controls.}

\item{sigmagam}{Optional list, usually from \code{\link[=exal_make_vb_sigmagam_control]{exal_make_vb_sigmagam_control()}}.}

\item{sts}{Optional list, usually from \code{\link[=exal_make_vb_sts_control]{exal_make_vb_sts_control()}}, for the
dynamic latent \code{s_t} warmup block used by \code{\link[=exdqlmLDVB]{exdqlmLDVB()}}.}

\item{rhs}{Optional nested RHS warmup block. Supported keys include
\code{freeze_tau_iters}, \code{freeze_tau_warmup_iters}, \code{update_every},
\code{update_every_warmup}, \code{update_every_warmup_iters}, \code{beta_presteps},
\code{beta_presteps_iters}, \code{gradcheck}, \code{gradcheck_iters}, \code{gradcheck_h},
\code{tau_local_tol}, \code{min_tau_updates}, \code{max_tau_updates},
\code{force_tau_after_warmup}, and \code{recompute_elbo_after_tau_update}.}

\item{diagnostics}{Optional diagnostics block. Supported keys include
\code{rhs_trace}, \code{rhs_deep}, \code{rhs_trace_thresholds}, \code{rhs_trace_top_k}, and
\code{rhs_trace_eps}.}
}
\value{
A normalized list suitable for \code{vb_control}.
}
\description{
Returns a normalized \code{vb_control} list suitable for \code{\link[=exal_ldvb_fit]{exal_ldvb_fit()}} and
\code{vb_args$vb_control} in \code{\link[=qdesn_fit_vb]{qdesn_fit_vb()}}. This collects the main advanced
warmup and RHS scheduling options in one readable builder.
}
\examples{
exal_make_vb_control()
exal_make_vb_control(
  max_iter = 200L,
  sigmagam = exal_make_vb_sigmagam_control(freeze_warmup_iters = 15L),
  sts = exal_make_vb_sts_control(freeze_warmup_iters = 10L),
  rhs = list(
    freeze_tau_warmup_iters = 20L,
    update_every_warmup = 4L,
    force_tau_after_warmup = TRUE
  )
)
}
