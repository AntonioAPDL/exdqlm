% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_selection_distribution_first.R
\name{model_selection_distribution_first}
\alias{model_selection_distribution_first}
\title{Distribution-first model selection for Q-DESN (shared spec across quantiles)}
\usage{
model_selection_distribution_first(
  y,
  ppt,
  soil,
  stage = c("coarse", "final"),
  p_vec = c(0.05, 0.5, 0.95),
  seed_vec = c(42L, 101L),
  parallel = FALSE,
  n_workers = max(1L, parallel::detectCores() - 1L),
  keep_artifacts = TRUE,
  plot = FALSE,
  progress_console = FALSE,
  progress_log = NULL,
  progress_every = 1L,
  grid_preset = c("default", "small", "tiny", "micro"),
  max_specs = Inf,
  grid_seed = 123L
)
}
\arguments{
\item{y}{Numeric vector, response (USGS).}

\item{ppt}{Numeric vector, precipitation covariate (same length as \code{y}).}

\item{soil}{Numeric vector, soil covariate (same length as \code{y}).}

\item{stage}{Character, either \code{"coarse"} (fast) or \code{"final"} (thorough).}

\item{p_vec}{Numeric vector of quantiles to fit jointly. Default: \code{c(0.05, 0.50, 0.95)}.}

\item{seed_vec}{Integer vector of reservoir seeds to average across (reduces variance).
Default: \code{c(42L, 101L)}.}

\item{parallel}{Logical, use base parallel (PSOCK) across candidate specs. Default: \code{FALSE}.}

\item{n_workers}{Integer, number of workers if \code{parallel=TRUE}. Default: \code{max(1, parallel::detectCores()-1)}.}

\item{keep_artifacts}{Logical, keep exemplar fit objects/draws for the winner. Default: \code{TRUE}.}

\item{plot}{Logical, make quick ggplot diagnostics for winner (requires \pkg{ggplot2}). Default: \code{FALSE}.}

\item{progress_console}{Logical, print progress to console. Default: \code{FALSE}.}

\item{progress_log}{Optional path to a logfile to append progress lines. Default: \code{NULL}.}

\item{progress_every}{Integer, how often to log in non-parallel mode (by spec index). Default: \code{1L}.}

\item{grid_preset}{Character, one of \code{"default"}, \code{"small"}, \code{"tiny"}, \code{"micro"} for built-in grids. Default: \code{"default"}.}

\item{max_specs}{Integer, optional cap to randomly sample at most this many specs from the grid. Default: \code{Inf}.}

\item{grid_seed}{Integer RNG seed used when subsampling the grid. Default: \code{123L}.}
}
\value{
A list with elements:
\itemize{
\item \code{leaderboard}: data.frame with CRPS and diagnostics per spec.
\item \code{winner}: key string for the best spec.
\item \code{winner_bundle}: list with winner details (fits/synth on scoring window)
when \code{keep_artifacts=TRUE}; otherwise minimal.
}
}
\description{
For each candidate \emph{shared} specification, fit Q-DESN readouts at
\code{p_vec} (default \eqn{\{0.05, 0.50, 0.95\}}), synthesize a single predictive
distribution (isotonic + optional rearrangement), and select the spec with the
lowest CRPS (approximated via averaged pinball loss on the empirical quantile
grid extracted from synthesized draws).
}
\details{
This function uses internal helpers only and \strong{does not} attach or install any packages.
It relies on objects in this package namespace: \code{qdesn_fit_vb()},
\code{posterior_predict.qdesn_fit()}, \code{exal_static_LDVB()},
and \code{exdqlm_synthesize_from_draws()}.
}
