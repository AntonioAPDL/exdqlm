% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_ts_mc_quantiles.R
\name{simulate_ts_mc_quantiles}
\alias{simulate_ts_mc_quantiles}
\title{Simulate Time Series + Monte Carlo Conditional Quantiles (per time t)}
\usage{
simulate_ts_mc_quantiles(
  T,
  p_grid = sort(unique(c(seq(0.1, 0.95, by = 0.05), 0.5))),
  R_mc = 2000L,
  scenario = c("ar1_exal", "sin_exal", "hetero_exal", "regime_exal", "ar1_t"),
  params = NULL,
  burnin = 500L,
  seed = 123L,
  keep_latents = FALSE,
  keep_draws = FALSE
)
}
\arguments{
\item{T}{Integer; desired post-burn-in length.}

\item{p_grid}{Numeric vector of quantile levels in (0,1). Duplicates are removed,
sorted, and used for each t. Defaults to c(0.10, 0.15, ..., 0.90, 0.95, 0.50).}

\item{R_mc}{Integer; Monte Carlo draws per time t to estimate the conditional quantiles.}

\item{scenario}{Character; one of c("ar1_exal","sin_exal","hetero_exal","regime_exal","ar1_t").}

\item{params}{Named list of scenario parameters. See Details below for defaults.}

\item{burnin}{Integer; extra steps simulated and discarded as washout (default 500).}

\item{seed}{Integer; RNG seed for reproducibility.}

\item{keep_latents}{Logical; if TRUE and exAL scenario, returns \eqn{\mu_t}, \eqn{\sigma_t}.}

\item{keep_draws}{Logical; if TRUE, returns per-time Monte Carlo draws (large!).}
}
\value{
A list with
\itemize{
\item \code{y}: numeric vector (length T) of the observed series (post-burn-in).
\item \code{q}: numeric matrix (T x K) of MC quantile estimates; columns match \code{p}.
\item \code{p}: the quantile levels actually used.
\item \code{info}: list with \code{scenario}, \code{params}, \code{burnin}, \code{R_mc}, \code{seed}.
\item \code{extras} (optional): for exAL scenarios and \code{keep_latents=TRUE},
returns \code{mu}, \code{sigma_t}; and if \code{keep_draws=TRUE}, a list of length T
with the MC draws for each t (warning: large).
}
}
\description{
Generates an observed time series \eqn{y_{1:T}} under several non-trivial
dynamics and, for each time step \eqn{t}, approximates the conditional
quantiles \eqn{Q_p(y_t \mid \mathcal F_{t-1})} by Monte Carlo draws from the
model-specific conditional distribution of \eqn{y_t} given the realized past.
}
\details{
Built-in scenarios (set via \code{scenario}):
\itemize{
\item \code{"ar1_exal"}:       \eqn{y_t = \phi_1 y_{t-1} + \epsilon_t^{exAL}(p_0, \sigma, \gamma)}
\item \code{"sin_exal"}:       \eqn{y_t = a_1 y_{t-1} + b_1 \sin(2\pi t/P) + c + \epsilon_t^{exAL}}
\item \code{"hetero_exal"}:    \eqn{y_t = a_1 y_{t-1} + \epsilon_t^{exAL}(\sigma_t)}, \ \(\sigma_t = \sigma_0 + \sigma_1\,\mathrm{logit}^{-1}(\kappa y_{t-1})\)
\item \code{"regime_exal"}:    \eqn{y_t = a_1 y_{t-1} + b\,\max(0, \sin(2\pi t/P)) + \epsilon_t^{exAL}}
\item \code{"ar1_t"}:          \eqn{y_t = \phi_1 y_{t-1} + \tau\,t_\nu} (Student-t noise; shows non-exAL)
}

For \verb{*_exal} scenarios, the exAL(p0, sigma, gamma) noise is simulated via the
Gaussian–mean–variance mixture:
\deqn{ s \sim \mathcal N^+(0,1),\quad v \sim \mathrm{Exp}(\text{rate}=1/\sigma),\quad
       y_t \mid \cdot \sim \mathcal N\!\big(\mu_t + \lambda(\gamma)\sigma s + A(\gamma)v,\; B(\gamma)\sigma v\big), }
where \eqn{A,B,\lambda} follow your definitions and implicitly depend on \eqn{p_0}.

\strong{Defaults per scenario (override via \code{params}):}
\itemize{
\item \code{ar1_exal}:  \code{list(p0=0.5, phi1=0.6, sigma=1.0, gamma=0.0)}
\item \code{sin_exal}:  \code{list(p0=0.5, a1=0.5, b1=0.3, period=50, c=0.0, sigma=1.0, gamma=0.0)}
\item \code{hetero_exal}: \code{list(p0=0.5, a1=0.5, sigma0=0.3, sigma1=0.4, kappa=1.0, gamma=0.0)}
\item \code{regime_exal}: \code{list(p0=0.5, a1=0.6, b=0.8, period=20, sigma=1.0, gamma=0.0)}
\item \code{ar1_t}:     \code{list(phi1=0.6, tau=1.0, nu=5)}
}

Quantiles are computed by \code{stats::quantile(..., type = 7, names = FALSE)}.
}
