% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_esn_pipeline.R
\name{run_esn_pipeline_from_cfg}
\alias{run_esn_pipeline_from_cfg}
\title{Run ESN quantile pipeline from a configuration list}
\usage{
run_esn_pipeline_from_cfg(
  cfg,
  file_long,
  file_obs = NULL,
  out_dir,
  repo_root = NULL,
  save_outputs = TRUE,
  rscript = "Rscript",
  pipeline_script = NULL,
  verbose = TRUE
)
}
\arguments{
\item{cfg}{list; full configuration (defaults + spec + overrides).}

\item{file_long}{character; path to the long-format data file used by the pipeline.}

\item{file_obs}{character or NULL; optional observed-data file (for real-data mode).}

\item{out_dir}{character; directory where the pipeline should write outputs.}

\item{repo_root}{character or NULL; root of the git repo. If NULL, attempt to
detect it via \code{git rev-parse --show-toplevel}.}

\item{save_outputs}{logical; whether to allow the pipeline to write figures/tables.
This sets the \code{EXDQLM_SAVE_OUTPUTS} flag; the YAML \code{cfg$outputs}
can still override it.}

\item{rscript}{character; path to the Rscript executable (default "Rscript").}

\item{pipeline_script}{character; relative path from \code{repo_root} to the
main pipeline script (default "scripts/pipeline_sim_main.R").}

\item{verbose}{logical; if TRUE, print the command and capture+echo the script
output line by line.}
}
\value{
Invisibly, a list with components \code{status} (integer exit status),
\code{cmd} (character vector with the Rscript command), and \code{stdout}
(captured output as character vector) when \code{verbose = TRUE}.
On non-zero exit status, a warning is emitted.
}
\description{
This is a thin wrapper around \code{scripts/pipeline_sim_main.R} that:
\itemize{
\item accepts a parsed YAML/JSON configuration list \code{cfg},
\item serializes it to JSON and passes it via \code{EXDQLM_CFG_JSON},
\item sets file paths and output directory via environment variables,
\item runs the pipeline script in a separate R process,
\item optionally checks for errors and returns invisibly.
}
}
\details{
It is primarily used by the ESN model-selection routines.
}
