% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exal_online_stage0.R
\name{exal_online_stage0_benchmark}
\alias{exal_online_stage0_benchmark}
\title{Deterministic Stage-0 baseline benchmark for online VB-LD}
\usage{
exal_online_stage0_benchmark(
  seed = 20260223L,
  n = 72L,
  k = 5L,
  t0 = 48L,
  p0 = 0.5,
  ridge_tau2 = 5,
  batch_vb_control = list(max_iter = 35L, tol = 1e-04, tol_par = 1e-04, verbose = FALSE),
  online_control = list(M = 2L, K = 4L, W = 24L, L_loc = 2L, window_passes = 1L, jitter =
    1e-10),
  check_repro = TRUE,
  return_trace = TRUE
)
}
\arguments{
\item{seed}{Integer fixture seed.}

\item{n}{Integer sample size for fixture.}

\item{k}{Integer design dimension (includes intercept column).}

\item{t0}{Integer warm-start prefix size used for online initialization.}

\item{p0}{Quantile level in (0,1).}

\item{ridge_tau2}{Ridge prior variance used in the benchmark.}

\item{batch_vb_control}{List of controls forwarded to \code{exal_ldvb_fit()}.}

\item{online_control}{List of controls forwarded to \code{exal_online_init()}.}

\item{check_repro}{Logical; if \code{TRUE}, run the benchmark twice and compare hashes.}

\item{return_trace}{Logical; if \code{TRUE}, include per-step trace for each run.}
}
\value{
List with benchmark config, run summaries, and reproducibility check.
}
\description{
Runs a fixed-seed benchmark that compares:
\itemize{
\item full batch VB-LD on all data,
\item online VB-LD initialized on a prefix and updated sequentially.
}
}
\details{
The benchmark reports alignment metrics, health diagnostics, timings, and
a deterministic hash. Optionally, it runs the same benchmark twice and checks
hash equality to validate reproducibility.
}
