% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exal_inference_config.R
\name{exal_make_precision_beta_control}
\alias{exal_make_precision_beta_control}
\title{Build precision-beta MCMC stabilization control}
\usage{
exal_make_precision_beta_control(
  preset = c("recommended", "off", "ladder_v1", "ladder_v2", "eigen_v1"),
  enabled = NULL,
  symmetrize = NULL,
  jitter_ladder = NULL,
  eigen_fallback = NULL,
  eigen_floor_abs = NULL,
  eigen_floor_rel = NULL,
  trace = NULL
)
}
\arguments{
\item{preset}{One of \code{"recommended"}, \code{"off"}, \code{"ladder_v1"},
\code{"ladder_v2"}, or \code{"eigen_v1"}. \code{"recommended"} resolves to
\code{"ladder_v2"}.}

\item{enabled}{Optional logical override.}

\item{symmetrize}{Optional logical override.}

\item{jitter_ladder}{Optional numeric jitter ladder override.}

\item{eigen_fallback}{Optional logical override.}

\item{eigen_floor_abs, eigen_floor_rel}{Optional positive numeric overrides
for the eigen fallback floor.}

\item{trace}{Optional logical override.}
}
\value{
A normalized list suitable for \code{mcmc_control$precision_beta}.
}
\description{
Returns a normalized \code{mcmc_control$precision_beta} block for
\code{\link[=exal_mcmc_fit]{exal_mcmc_fit()}} and \code{\link[=qdesn_fit_mcmc]{qdesn_fit_mcmc()}}. Use a named preset for the common
recovery policies that were validated on the hardest Q-DESN ridge failures,
or pass explicit numeric overrides when you need a custom policy.
}
\details{
\code{preset = "ladder_v2"} is the recommended default: it uses symmetric
jittered Cholesky repair with a stronger ladder up to \code{1e-2}.

\code{preset = "eigen_v1"} keeps a lighter jitter ladder and enables the
eigenvalue-floored SPD fallback for the hardest residual precision failures.
}
\examples{
exal_make_precision_beta_control()
exal_make_precision_beta_control("eigen_v1")
exal_make_precision_beta_control("ladder_v2", jitter_ladder = c(0, 1e-8, 1e-4, 1e-2))
}
