% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qdesn_vb.R
\name{qdesn_fit_vb}
\alias{qdesn_fit_vb}
\title{Q-DESN (Quantile Deep Echo State Network) via exAL-LDVB Readout}
\usage{
qdesn_fit_vb(
  y,
  p0,
  D = 3L,
  n = c(200L, 100L, 50L),
  n_tilde = c(50L, 25L),
  m = 12L,
  input_mode = c("raw_y_lags", "dlm_decomp_lags"),
  decomposition = NULL,
  decomposition_xreg = NULL,
  standardize_inputs = FALSE,
  input_bound = c("none", "tanh"),
  win_scale_global = 1,
  win_scale_bias = 1,
  win_scale_lags = NULL,
  alpha = 0.3,
  rho = rep(0.9, length.out = D),
  act_f = "tanh",
  act_k = "identity",
  pi_w = 0.1,
  pi_in = 0.1,
  w_dist = function(n) rnorm(n, 0, 1),
  in_dist = function(n) rnorm(n, 0, 1),
  washout = 100L,
  add_bias = FALSE,
  weights = NULL,
  state_noise_sd = 0,
  segments = NULL,
  seed = NULL,
  vb_args = list(),
  fit_readout = TRUE
)
}
\arguments{
\item{y}{Numeric vector, length T (univariate series).}

\item{p0}{Target quantile in (0,1).}

\item{D}{Integer depth (\eqn{D \ge 1}).}

\item{n}{Integer vector of length D with reservoir sizes \eqn{n_d}.}

\item{n_tilde}{Integer vector of length D-1 with reducers \eqn{\tilde n_d} (ignored if D=1).}

\item{m}{Integer number of lags in \eqn{u_t=(1, y_{t-1},..., y_{t-m})}.}

\item{decomposition_xreg}{Optional numeric matrix/data.frame of covariates
used only by decomposition mode when regression/transfer blocks are enabled.}

\item{alpha}{Leak in \eqn{(0,1)}.}

\item{rho}{Numeric vector length D, spectral scales \eqn{\rho_d \in (0,1)}.}

\item{act_f}{Activation for reservoir pre-activations (string or function): "tanh","relu","identity" or function.}

\item{act_k}{Activation applied elementwise to reduced lower-layer states in the stack (same choices).}

\item{pi_w, pi_in}{Sparsity probs in (0,1) for internal and input matrices.}

\item{w_dist, in_dist}{Functions that generate weight entries (default \code{rnorm}).}

\item{washout}{Integer >=0; additional initial samples to drop after lag m to allow state settling.}

\item{add_bias}{Logical; if TRUE, appends a constant 1 column to the readout design X.}

\item{seed}{Optional integer to make the reservoir repeatable.}

\item{vb_args}{Named list forwarded to \code{exalStaticLDVB} (e.g. b0, V0,
a_sigma, b_sigma, max_iter, tol, n_samp_xi, ...). If
\code{vb_args$beta_prior_type} is omitted, Q-DESN defaults to
\code{"rhs_ns"}. For RHS-family priors, \code{shrink_intercept} is
enforced as \code{FALSE}. Advanced VB controls may be supplied directly as
\code{vb_args$vb_control}, or more explicitly via
\code{\link[=exal_make_vb_control]{exal_make_vb_control()}} together with
\code{\link[=exal_make_vb_sigmagam_control]{exal_make_vb_sigmagam_control()}}.}
}
\value{
A list with:
\itemize{
\item \code{fit}: return of \code{exalStaticLDVB}.
\item \code{X}: design matrix used in the VB fit (post-washout).
\item \code{y_fit}: response vector aligned with \code{X}.
\item \code{mu_hat}: fitted \eqn{\hat \mu_t = x_t' \hat \beta} (in-sample, post-washout).
\item \code{reservoir}: the fixed reservoir (weights, reducers, hyperparams).
\item \code{states}: a compact list with \code{h} (last-layer states) and \code{h_all} (optional) per layer.
\item \code{meta}: indices kept, p0, sizes, etc.
}
}
\description{
Implements the model in your LaTeX: a deep, leaky reservoir with spectral
normalization produces features X_t; a linear readout \eqn{\mu_t = x_t' \beta}
is fitted under \eqn{exAL_{p0}} noise using your \code{exalStaticLDVB()}.
}
\section{Pipeline}{

\enumerate{
\item Build (or accept) a DESN reservoir with fixed random sparse weights.\cr
\item Roll deterministic states \eqn{h_{t,d}} from the observed series.\cr
\item Stack features \eqn{x_t = [h_{t,D}; k(\tilde h_{t,1}); ...; k(\tilde h_{t,D-1})]}\cr
\item Drop the first \code{max(m, washout)} points (lags and state transient).\cr
\item Fit \eqn{\beta,\sigma,\gamma} with \code{exalStaticLDVB()} for a single p0.\cr
}
}

