% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exal_online_vbld.R
\name{exal_online_fit}
\alias{exal_online_fit}
\title{Fit exAL with optional online VB-LD updates (single quantile)}
\usage{
exal_online_fit(
  y,
  X,
  p0,
  gamma_bounds,
  control = list(),
  vb_control = NULL,
  max_iter = NULL,
  tol = NULL,
  tol_par = NULL,
  n_samp_xi = NULL,
  verbose = NULL,
  init = list(),
  prior_gamma = NULL,
  prior_gamma_mu0 = NULL,
  prior_gamma_s20 = NULL,
  log_prior_gamma = NULL,
  prior_sigma = NULL,
  a_sigma = NULL,
  b_sigma = NULL,
  beta_prior_obj = NULL,
  ...
)
}
\arguments{
\item{y}{Numeric response vector.}

\item{X}{Numeric design matrix.}

\item{p0}{Quantile level in (0,1).}

\item{gamma_bounds}{Numeric length-2 bounds for gamma.}

\item{control}{Online controls list:
\code{enabled}, \code{strict}, \code{M}, \code{K}, \code{W}, \code{L_loc}, \code{window_passes},
\code{maxit_sigmagam}, \code{jitter}, \code{warm_start_n}, \code{warm_start_frac},
\code{keep_trace}, \code{update_rhs}, \code{update_sigmagam}.}

\item{vb_control, max_iter, tol, tol_par, n_samp_xi, verbose, init, prior_gamma, }{prior_gamma_mu0,prior_gamma_s20,log_prior_gamma,prior_sigma,a_sigma,b_sigma,
beta_prior_obj,... Same as \code{exal_ldvb_fit()}.}
}
\value{
\code{exal_vb} object. If online mode is enabled, diagnostics are stored
in \code{fit$misc$online}.
}
\description{
This wrapper preserves the batch interface from \code{exal_ldvb_fit()} and adds an
optional online path controlled by \code{control$enabled}. When online mode is
enabled, the function:
\enumerate{
\item runs batch LDVB on an initialization prefix,
\item streams the remaining observations through \code{exal_online_run()},
\item returns an \code{exal_vb}-compatible object so downstream offline code remains unchanged.
}
}
