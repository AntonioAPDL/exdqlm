% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/exdqlmFit_methods.R
\name{plot.exdqlmFit}
\alias{plot.exdqlmFit}
\title{Plot Method for Dynamic \code{exdqlmFit} Objects}
\usage{
\method{plot}{exdqlmFit}(
  x,
  type = c("quantile", "component", "state"),
  index = NULL,
  add = FALSE,
  col = "purple",
  cr.percent = 0.95,
  xlim = NULL,
  ylim = NULL,
  xlab = "time",
  ylab = NULL,
  lwd = 1.5,
  lwd.interval = 0.75,
  lty.interval = 2,
  ...
)
}
\arguments{
\item{x}{A fitted dynamic \code{exdqlmFit} object.}

\item{type}{Character string specifying the plot type. Use
\code{"quantile"} for the fitted dynamic quantile, \code{"component"} for
the contribution of a block of state elements, or \code{"state"} for a
single state element.}

\item{index}{Required for \code{type = "component"} or \code{type = "state"}.
For \code{type = "state"}, \code{index} much have length 1 indicating a
single element of the state vector to be plot. For \code{type = "component"},
\code{index} should be consecutive state indices in \eqn{\{1,\dots,q\}}
indicating the component to be plot.}

\item{add}{Optional logical value indicating whether the estimate will be
added to existing plot. Default is \code{FALSE}.}

\item{col}{Optional character vector of length 1 giving color of the
estimate to be plotted. Default is \code{purple}.}

\item{cr.percent}{Optional numeric in \code{(0, 1)} indicating the
probability mass for the credible intervals (e.g., \code{0.95}). Default \code{0.95}.}

\item{xlim, ylim}{Optional limits passed to the base plotting call.}

\item{xlab, ylab}{Optional axis labels passed to the base plotting call.}

\item{lwd, lwd.interval}{Line widths for the estimate and credible interval
bounds, respectively.}

\item{lty.interval}{Line type for the credible interval bounds.}

\item{...}{Additional arguments.}
}
\value{
Invisibly returns a list of the following:
\itemize{
\item \code{map.quant} - MAP estimate of the dynamic estimate.
\item \code{lb.quant} - Lower bound of the 95\% CrIs of the dynamic estimate.
\item \code{ub.quant} - Upper bound of the 95\% CrIs of the dynamic estimate.
\item \code{x} - Time/index values used for plotting.
}
}
\description{
Plot fitted dynamic quantiles, fitted component contributions, or individual
state elements from a dynamic fit. The default \code{type = "quantile"}
delegates to \code{\link{exdqlmPlot}}. Component and state views delegate to
\code{\link{compPlot}}.
}
\examples{
\donttest{
data("scIVTmag", package = "exdqlm")
old = options(exdqlm.max_iter = 15L)
y = scIVTmag[1:80]
trend.comp = polytrendMod(2, rep(mean(y), 2), 10*diag(2))
seas.comp = seasMod(365, c(1, 2), C0 = 10*diag(4))
model = trend.comp + seas.comp
M0 = exdqlmLDVB(y, p0 = 0.85, model, df = c(0.98, 1), dim.df = c(2, 4),
                   gam.init = -3.5, sig.init = 15,
                   n.samp = 20, tol = 0.2, verbose = FALSE)
# plot quantile
plot(M0)
# plot first harmonic component
plot(M0, type="component", index = c(3, 4), col = "blue")
options(old)
}
}
