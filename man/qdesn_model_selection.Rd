% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qdesn_model_selection_facade.R
\name{qdesn_model_selection}
\alias{qdesn_model_selection}
\title{Authoritative Q-DESN model-selection entry point}
\usage{
qdesn_model_selection(
  dataset_id = NULL,
  file_long = NULL,
  file_obs = NULL,
  base_cfg = NULL,
  ms_cfg = NULL,
  out_root = NULL,
  repo_root = NULL,
  verbose = TRUE,
  cfg = NULL,
  ds = NULL,
  run_dir = NULL,
  engine = "auto"
)
}
\arguments{
\item{dataset_id}{Character identifier used for logging and output naming.}

\item{file_long}{Optional path to long-format input data. Required for legacy
model selection and accepted as a shorthand dataset source for v2.}

\item{file_obs}{Optional observed-data file for legacy real-data workflows.}

\item{base_cfg}{Base configuration list for legacy workflows, or a partial
base configuration to merge with \code{ms_cfg} when \code{cfg} is not supplied.}

\item{ms_cfg}{Configuration list containing \code{model_selection}.}

\item{out_root}{Output root. Used to derive \code{run_dir} when \code{run_dir} is not
supplied.}

\item{repo_root}{Repository root forwarded to the legacy selector.}

\item{verbose}{Emit progress messages.}

\item{cfg}{Full modern v2 configuration. Preferred for new code.}

\item{ds}{Dataset descriptor for v2, with at least \code{input_path} and \code{mode}.}

\item{run_dir}{Explicit v2 run directory.}

\item{engine}{One of \code{"auto"}, \code{"v2"}, or \code{"legacy"}.}
}
\value{
Legacy runs return the historical tuning list. V2 runs return the
result from \code{run_model_selection_v2()} with \code{engine}, \code{run_dir}, and
\code{dataset_id} metadata added.
}
\description{
\code{qdesn_model_selection()} is the public facade for Q-DESN model selection.
Modern staged configurations with \code{model_selection$stages} are routed to the
v2 engine. Legacy configurations with \code{model_selection$esn_space} are routed
to the historical ESN-pipeline selector for backward compatibility.
}
\details{
The v2 engine is the preferred path for current Q-DESN work because it supports
staged candidate grids, current DESN configuration normalization, ridge/RHS/RHS_NS
beta-prior construction, origin-mode scoring, synthesis diagnostics, and progress
tracking. The legacy path is retained only for old configuration files that still
define \code{model_selection$esn_space}.
}
