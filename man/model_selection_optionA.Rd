% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model_selection_distribution_first.R,
%   R/optionA_alias.R
\name{model_selection_optionA}
\alias{model_selection_optionA}
\title{Option A: Split-once, path-forecast validation selection, then refit & test}
\usage{
model_selection_optionA(...)

model_selection_optionA(...)
}
\arguments{
\item{y, ppt, soil}{Numeric vectors (same length).}

\item{p_vec}{Quantiles to fit/synthesize. Default: c(0.05,0.50,0.95).}

\item{split}{Proportions c(train, val, test). Default: c(.80,.15,.05).}

\item{weight_leads}{"uniform" or "inverse_h" aggregation across horizons.}

\item{stage}{"coarse" or "final" affects VB and sampling budgets.}

\item{grid_preset}{As in your other selector ("default","small","tiny","micro").}

\item{seed_vec}{Vector of reservoir seeds to average per spec (variance reduction).}

\item{parallel, n_workers}{Same semantics as before.}

\item{keep_artifacts}{Keep winner's fits, forecasts, and draws (TRUE/FALSE).}

\item{progress_console, progress_log, progress_every}{Logging controls.}
}
\value{
list with leaderboard (validation), winner (key), selection_bundle with
validation details, and test_bundle (refit Train∪Val, score on Test).

See \code{model_selection_distribution_first()}.
}
\description{
Implements the end-to-end workflow:
Train (~80\%) | Validate (~15\%) | Test (~5\%).
Fit once per quantile on Train; recursive multi-horizon forecasts on Val;
synthesize (p = {0.05,0.50,0.95}); score CRPS by lead and aggregate;
select best spec; refit on Train∪Val; forecast Test; report Test CRPS.

Thin wrapper so scripts can call \code{exdqlm::model_selection_optionA()}.
It forwards all arguments to \code{model_selection_distribution_first()}.
}
