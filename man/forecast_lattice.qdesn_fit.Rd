% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qdesn_vb.R
\name{forecast_lattice.qdesn_fit}
\alias{forecast_lattice.qdesn_fit}
\title{Lattice multi-step forecasts for a Q-DESN fit using posterior predictive draws}
\usage{
forecast_lattice.qdesn_fit(
  object,
  y_all,
  origins,
  H,
  nd = 1000L,
  xreg_all = NULL,
  y_obs_last = NULL,
  lead_weights = NULL,
  mix_nd = NULL,
  chunk = 256L,
  seed = NULL,
  keep_origin_draws = TRUE,
  draws = NULL
)
}
\arguments{
\item{object}{qdesn_fit (from qdesn_fit_vb or compatible structure)}

\item{y_all}{numeric vector of observed y values (length >= max(origins))}

\item{origins}{integer vector of forecast origins (absolute indices into y_all)}

\item{H}{forecast horizon (steps ahead)}

\item{nd}{number of posterior draws per origin}

\item{xreg_all}{optional named list of exogenous series (length >= max(origins)+H)}

\item{y_obs_last}{last observed y index (defaults to length(y_all))}

\item{lead_weights}{optional numeric vector length H (base weights by lead)}

\item{mix_nd}{number of mixture draws per target (defaults to nd)}

\item{keep_origin_draws}{if FALSE, drop per-origin draws after mixture}

\item{draws}{optional posterior draws list from exal_vb_posterior_draws()}
}
\value{
list with per-origin draws (optional) and mixture draws per target
}
\description{
Lattice multi-step forecasts for a Q-DESN fit using posterior predictive draws
}
