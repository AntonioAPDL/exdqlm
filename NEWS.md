# exdqlm (development version)

# exdqlm 0.1.4
- Hygiene update: fix accidental S3 NOTE (rename internal helper `log.g` to `log_g`).
- Refresh documentation metadata (RoxygenNote).
- Add minimal smoke test and GitHub Actions CI.
- Ignore `.Rhistory`/`.RData` in git.
