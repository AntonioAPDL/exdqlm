# Suite-level switches that sit above defaults
suite_name: sim_suite_dlm
results_root: results

orchestrate:
  concurrency: 3       # jerez has headroom; tune if needed
  threads_per_proc: 1
  skip_if_success: true
  resume_failed: false

tmux:
  session_prefix: esn-
